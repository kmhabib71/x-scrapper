Summary of exactly what you want (clear & complete):You want a single, standalone Python script (generated by Claude) that:Runs automatically every 30–60 minutes on a completely free schedule (your preference: Vercel if possible, or Fly.io / GitHub Actions — I’ll give the best free option below).
Uses only your existing X Premium account cookies (no official X API, no paid scraper service — 100% free data access).
Searches X with a smart advanced query for real hiring intent (“hiring web developer”, “need web dev”, “looking for frontend”, etc.).
Detects new posts only (stores seen post IDs in a tiny SQLite DB so you never get duplicates).
For each new post, calls Claude API (your Anthropic key) to:Quickly confirm it’s a genuine hiring lead.
Generate two curated replies: one short comment + one polite DM version (value-first, non-spammy, referencing the post).

Sends you one clean notification (Telegram bot or email) containing:Post link + full text + username
The two Claude-generated replies (ready to copy-paste)

You manually review → click the link on X → post the reply yourself (no auto-posting).
Total ongoing cost = only your Claude API calls (literally pennies per month at this volume). Everything else is free forever.

Best free hosting in 2026 (zero cost):GitHub Actions = the winner (truly free cron, no credit card, runs hourly or every 15 min easily). Just push the script + a tiny workflow YAML.
Vercel (your main choice) is possible but requires a tiny workaround: turn the script into a FastAPI endpoint and use the free external service cron-job.org to ping it hourly (still $0). GitHub Actions is simpler and more reliable for pure scheduled scripts.
Fly.io = not truly free anymore for new accounts (small charges after trial).

I’ll give you the exact Claude prompt below. Paste it as-is into Claude (use Claude 3.5 Sonnet or Opus for best results). It will output the complete ready-to-run script + deployment instructions.Ready-to-copy prompt for Claude:

You are an expert Python automation engineer. Write a complete, production-ready, single-file Python script (Python 3.11+) that does the following:

1. **Configuration via environment variables** (use os.environ or python-dotenv):
   - ANTHROPIC_API_KEY
   - X_AUTH_TOKEN (your X Premium auth_token cookie)
   - X_CT0 (your X ct0/csrf cookie)
   - X_GUEST_ID and X_TWID (optional but recommended)
   - TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID (for notifications) OR fallback to SMTP email settings
   - SEARCH_INTERVAL_MINUTES = 30 (default)

2. **X Scraping** (no paid API, no external libraries beyond httpx, beautifulsoup4, and sqlite3):
   - Use httpx with your X Premium cookies + proper headers to hit X's internal advanced search GraphQL endpoint (or the search URL that works in 2026).
   - Search query must be: ("hiring" OR "looking for" OR "need a" OR "recommend" OR "freelance" OR "build me" OR "website") ("web developer" OR "web dev" OR "frontend developer" OR "full stack developer" OR "react developer" OR "next.js developer") -is:retweet lang:en
   - Fetch latest 20-30 results per run.
   - Parse post ID, username, text, full URL, timestamp.

3. **Deduplication**:
   - Use SQLite database (seen_posts.db) to store post IDs that have already been processed.

4. **AI Processing with Claude**:
   - For every new post, call Anthropic Claude API (use anthropic SDK).
   - First prompt: "Is this post someone actively looking to hire a web developer? Answer only YES or NO and one-sentence reason."
   - If YES, second prompt: "Write two short, professional, value-first replies for a web developer freelancer:
     - Reply A: short public comment (max 280 chars)
     - Reply B: polite DM offer
       Reference something specific from the post. Never sound spammy. Be helpful and mention relevant skills (React, Next.js, Tailwind, etc.)."

5. **Notification**:
   - Send one clean message via Telegram (preferred) or SMTP email.
   - Include: post URL, full post text, username, Claude's YES/NO + reason, and the two replies (clearly labeled).

6. **Main loop**:
   - Run as a script that can be triggered by cron/GitHub Actions.
   - Add random 5-15 second delays between requests to look natural.
   - Full error handling, logging, and comments.

7. **Extra**:
   - At the top of the script, include clear instructions on:
     - How to get the X cookies from Chrome DevTools (exact steps).
     - How to set up GitHub Actions workflow YAML (provide the full .yml content as a code block).
     - Alternative: how to make it a FastAPI endpoint for Vercel + cron-job.org ping.
   - Use only free/standard libraries: httpx, anthropic, sqlite3, os, json, datetime, time, smtplib (for email fallback). No Playwright (too heavy).

Output ONLY the complete script + the two extra code blocks (GitHub workflow and Vercel instructions). Do not add any extra explanation outside the code.

What to do next (after Claude gives you the code):Copy the full script.
Create a new GitHub repo → add the script + requirements.txt (Claude will tell you what to put).
Add your secrets in GitHub repo Settings → Secrets.
Push → the workflow will run on schedule automatically.

This will be rock-solid, near-zero cost, and exactly what you described. Just say “I pasted the prompt into Claude — here’s what it gave me” if you want me to review/debug the output, or tell me your preferred notification (Telegram vs email) and I’ll tweak the prompt further. You’ll have the full system live in under an hour.
